import React from "react"

const Articles = ({ count }) => (
  <div>
    Articles widget only has a 'count' variable, and the MVC widget controller
    must handle the data here.
  </div>
)

export default Articles
